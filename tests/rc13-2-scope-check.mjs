import fs from "node:fs";
import assert from "node:assert/strict";
import path from "node:path";
const root=path.resolve(process.argv[2]||"dist");
const read=file=>fs.readFileSync(path.join(root,file),"utf8");
const gate=read("access-gate.js");
const context=read("scope-context.js");
const repo=read("scope-repository.js");
const index=read("index.html");
assert.match(gate,/uid-profile-scope-canonical-artifact/);
assert.doesNotMatch(gate,/loadScopedApplication/);
assert.match(context,/filterData/);
assert.match(repo,/territorial-granular/);
assert.match(repo,/collection\("registros"\)/);
assert.match(repo,/collection\("cambios"\)/);
assert.match(repo,/installVirtualCloud/);
assert.match(index,/scope-context\.js/);
assert.match(index,/scope-repository\.js/);
assert.match(index,/scope-ui\.js/);
console.log("PASS alcance unificado: escritura granular con compatibilidad temporal del overlay");
