window.SmartRisk=window.SmartRisk||{};SmartRisk.Card={metric({label,value,note}){return `<article class="metric-card"><div class="metric-label">${label}</div><div class="metric-value">${value}</div><p class="metric-note">${note}</p></article>`},panel({title,badge="",body=""}){return `<article class="panel"><header class="panel-header"><h2 class="panel-title">${title}</h2>${badge?`<span class="badge">${badge}</span>`:""}</header><div class="panel-body">${body}</div></article>`}};