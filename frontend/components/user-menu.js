window.SmartRisk=window.SmartRisk||{};
SmartRisk.UserMenu={
 render(){const user=SmartRisk.UserContext.current();if(!user)return "";return `<div class="user-menu"><button id="user-menu-toggle" class="user-menu-toggle" type="button" aria-expanded="false"><span class="user-menu-avatar">${SmartRisk.Utils.escapeHtml?.(user.initials)||user.initials}</span><span class="user-menu-copy"><strong>${SmartRisk.Utils.escapeHtml?.(user.name)||user.name}</strong><small>${user.role}</small></span></button><div id="user-menu-panel" class="user-menu-panel" hidden><div class="user-menu-context">${SmartRisk.UserContext.territoryLabel(user)}</div><a href="#/usuarios" data-permission="user.manage">Administrar usuarios</a><a href="change-password.html">Cambiar contraseña</a><button id="user-logout" type="button">Cerrar sesión</button></div></div>`},
 bind(){const toggle=document.getElementById("user-menu-toggle"),panel=document.getElementById("user-menu-panel");toggle?.addEventListener("click",()=>{const open=panel.hidden;panel.hidden=!open;toggle.setAttribute("aria-expanded",String(open))});document.getElementById("user-logout")?.addEventListener("click",()=>SmartRisk.AuthService.logout());SmartRisk.RoleGuard.apply(document)}
};
