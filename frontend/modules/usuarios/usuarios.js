window.SmartRisk=window.SmartRisk||{};
(function(){
  const SR=SmartRisk;let filtered=[],selectedId="";
  const roles=["Administrador","Coordinador zonal","Analista","Técnico UGR","Consulta"],provinces=["Guayas","Los Ríos","Santa Elena","Bolívar","Galápagos"],statusLabel={active:"Activo",suspended:"Suspendido",blocked:"Bloqueado"};
  const badge=status=>status==="active"?"badge-success":status==="blocked"?"badge-danger":"badge-warning",esc=value=>SR.Utils.escapeHtml(String(value??""));
  function users(){return SR.UserManagementService.list()}
  function summary(){const all=users();return `<section class="user-summary"><article class="metric-card"><span>Total visible</span><strong>${all.length}</strong><small>Usuarios en tu ámbito</small></article><article class="metric-card"><span>Activos</span><strong>${all.filter(x=>x.status==="active").length}</strong><small>Con acceso habilitado</small></article><article class="metric-card"><span>Suspendidos</span><strong>${all.filter(x=>x.status==="suspended").length}</strong><small>Acceso temporalmente detenido</small></article><article class="metric-card"><span>Cambio pendiente</span><strong>${all.filter(x=>x.mustChangePassword).length}</strong><small>Contraseña temporal activa</small></article></section>`}
  function render(){filtered=users();selectedId=filtered[0]?.id||"";return `<section class="page-header"><div><span class="kicker">Seguridad institucional</span><h1 class="page-title">Gestión de usuarios</h1><p class="page-subtitle">Crea, asigna y controla las cuentas que acceden a SmartRisk.</p></div><button id="new-user" class="btn btn-primary">${SR.Icon.render("plus",17)} Nuevo usuario</button></section><div id="user-summary-root">${summary()}</div><section class="panel"><div class="panel-body"><div class="user-filter-grid"><div class="search-wrap"><span class="search-icon">${SR.Icon.render("search",18)}</span><input id="user-search" class="search-input" placeholder="Buscar por nombre, correo o institución…"></div><select id="user-role-filter" class="select-input"><option value="">Todos los roles</option>${roles.map(x=>`<option>${x}</option>`).join("")}</select><select id="user-status-filter" class="select-input"><option value="">Todos los estados</option><option value="active">Activo</option><option value="suspended">Suspendido</option><option value="blocked">Bloqueado</option></select><button id="user-reset" class="btn btn-secondary">Limpiar</button></div></div></section><section class="user-layout"><article class="panel"><header class="panel-header"><h2 class="section-title">Directorio de cuentas</h2><span id="user-count" class="badge">${filtered.length} registros</span></header><div class="panel-body"><div id="user-table"></div></div></article><article class="panel"><header class="panel-header"><h2 class="section-title">Detalle del usuario</h2></header><div id="user-detail" class="panel-body">${detail()}</div></article></section>`}
  function detail(){const user=users().find(x=>x.id===selectedId);if(!user)return `<div class="empty-state">Seleccione un usuario.</div>`;return `<div class="user-detail"><div class="user-detail-head"><span class="user-avatar">${esc(user.initials)}</span><div><h3>${esc(user.name)}</h3><p>${esc(user.email)}</p></div></div><div class="detail-row"><span>Estado</span><span class="badge ${badge(user.status)}">${statusLabel[user.status]||user.status}</span></div><div class="detail-row"><span>Rol</span><strong>${esc(user.role)}</strong></div><div class="detail-row"><span>Institución</span><strong>${esc(user.institution)}</strong></div><div class="detail-row"><span>Cargo</span><strong>${esc(user.position||"Sin registrar")}</strong></div><div class="detail-row"><span>Ámbito</span><strong>${esc(SR.UserContext.territoryLabel(user))}</strong></div><div class="detail-row"><span>Último acceso</span><strong>${user.lastAccessAt?new Date(user.lastAccessAt).toLocaleString("es-EC"):"Sin accesos"}</strong></div><div class="detail-row"><span>Contraseña</span><strong>${user.mustChangePassword?"Temporal; cambio pendiente":"Personal"}</strong></div><div class="user-actions"><button id="edit-user" class="btn btn-secondary">Editar</button><button id="reset-user-password" class="btn btn-secondary">Restablecer clave</button>${user.status==="active"?`<button id="suspend-user" class="btn btn-secondary">Suspender</button>`:`<button id="activate-user" class="btn btn-secondary">Activar</button>`}<button id="block-user" class="btn btn-secondary">Bloquear</button><button id="delete-user" class="btn btn-secondary user-danger">Eliminar</button></div></div>`}
  function draw(){document.getElementById("user-summary-root").innerHTML=summary();SR.Table.create({containerId:"user-table",columns:[{key:"name",label:"Funcionario"},{key:"email",label:"Correo"},{key:"role",label:"Rol"},{key:"institution",label:"Institución"},{key:"statusText",label:"Estado",type:"status"},{key:"id",label:"Detalle",type:"action"}],rows:filtered.map(x=>({...x,statusText:statusLabel[x.status]||x.status})),onAction:id=>{selectedId=id;document.getElementById("user-detail").innerHTML=detail();bindDetail()}});document.getElementById("user-count").textContent=`${filtered.length} registros`}
  function apply(){const q=SR.Utils.normalizeText(document.getElementById("user-search")?.value||""),role=document.getElementById("user-role-filter")?.value||"",status=document.getElementById("user-status-filter")?.value||"";filtered=users().filter(user=>SR.Utils.normalizeText(`${user.name} ${user.email} ${user.institution} ${user.role}`).includes(q)&&(!role||user.role===role)&&(!status||user.status===status));draw()}
  function form(user={}){const availableRoles=SR.UserContext.current().role==="Administrador"?roles:roles.filter(x=>x!=="Administrador"),t=user.territory||{};return `<div class="form-grid"><label class="form-field"><span class="form-label">Nombre completo *</span><input id="uf-name" class="form-input" value="${esc(user.name)}"></label><label class="form-field"><span class="form-label">Correo *</span><input id="uf-email" type="email" class="form-input" value="${esc(user.email)}"></label><label class="form-field"><span class="form-label">Institución</span><input id="uf-institution" class="form-input" value="${esc(user.institution)}"></label><label class="form-field"><span class="form-label">Cargo</span><input id="uf-position" class="form-input" value="${esc(user.position)}"></label><label class="form-field"><span class="form-label">Rol *</span><select id="uf-role" class="select-input">${availableRoles.map(x=>`<option ${user.role===x?"selected":""}>${x}</option>`).join("")}</select></label><label class="form-field"><span class="form-label">Estado</span><select id="uf-status" class="select-input"><option value="active" ${user.status==="active"?"selected":""}>Activo</option><option value="suspended" ${user.status==="suspended"?"selected":""}>Suspendido</option><option value="blocked" ${user.status==="blocked"?"selected":""}>Bloqueado</option></select></label><label class="form-field"><span class="form-label">Ámbito territorial</span><select id="uf-scope" class="select-input"><option value="zonal" ${t.scope==="zonal"?"selected":""}>Zonal</option><option value="provincial" ${t.scope==="provincial"?"selected":""}>Provincial</option><option value="cantonal" ${t.scope==="cantonal"?"selected":""}>Cantonal</option></select></label><label class="form-field"><span class="form-label">Provincias</span><input id="uf-provinces" class="form-input" list="province-options" value="${esc((t.provinces||provinces).join(", "))}"><datalist id="province-options">${provinces.map(x=>`<option value="${x}">`).join("")}</datalist></label><label class="form-field form-field-full"><span class="form-label">Cantones (separados por coma)</span><input id="uf-cantons" class="form-input" value="${esc((t.cantons||[]).join(", "))}"></label></div><p class="muted">Las cuentas nuevas reciben la contraseña temporal <strong>1234</strong> y deben cambiarla en su primer acceso.</p>`}
  function read(){const value=id=>document.getElementById(id).value.trim();return {name:value("uf-name"),email:value("uf-email"),institution:value("uf-institution"),position:value("uf-position"),role:value("uf-role"),status:value("uf-status"),scope:value("uf-scope"),provinces:value("uf-provinces"),cantons:value("uf-cantons")}}
  function openForm(user=null){SR.Modal.open({title:user?"Editar usuario":"Crear usuario",body:form(user||{}),confirmLabel:user?"Guardar cambios":"Crear cuenta",onConfirm:()=>{const result=user?SR.UserManagementService.update(user.id,read()):SR.UserManagementService.create(read());if(!result.ok){SR.Toast.show(result.message,"danger");return false}selectedId=result.user.id;SR.Toast.show(user?"Usuario actualizado.":"Usuario creado con contraseña temporal 1234.","success");SR.Router.refresh()}})}
  function confirm(title,message,action){SR.Modal.open({title,body:`<p>${message}</p>`,confirmLabel:"Confirmar",onConfirm:()=>{const result=action();SR.Toast.show(result.message||"Operación completada.",result.ok?"success":"danger");if(result.ok)SR.Router.refresh();return result.ok}})}
  function bindDetail(){const user=users().find(x=>x.id===selectedId);if(!user)return;document.getElementById("edit-user")?.addEventListener("click",()=>openForm(user));document.getElementById("reset-user-password")?.addEventListener("click",()=>confirm("Restablecer contraseña",`La cuenta de <strong>${esc(user.name)}</strong> volverá a usar la clave temporal <strong>1234</strong>.`,()=>SR.UserManagementService.resetPassword(user.id)));document.getElementById("suspend-user")?.addEventListener("click",()=>confirm("Suspender cuenta",`Se impedirá temporalmente el acceso de <strong>${esc(user.name)}</strong>.`,()=>SR.UserManagementService.setStatus(user.id,"suspended")));document.getElementById("activate-user")?.addEventListener("click",()=>confirm("Activar cuenta",`Se habilitará el acceso de <strong>${esc(user.name)}</strong>.`,()=>SR.UserManagementService.setStatus(user.id,"active")));document.getElementById("block-user")?.addEventListener("click",()=>confirm("Bloquear cuenta",`La cuenta de <strong>${esc(user.name)}</strong> quedará bloqueada.`,()=>SR.UserManagementService.setStatus(user.id,"blocked")));document.getElementById("delete-user")?.addEventListener("click",()=>confirm("Eliminar usuario",`Esta acción eliminará la cuenta de <strong>${esc(user.name)}</strong>.`,()=>SR.UserManagementService.remove(user.id)))}
  function bind(){draw();bindDetail();document.getElementById("new-user").onclick=()=>openForm();document.getElementById("user-search").oninput=apply;document.getElementById("user-role-filter").onchange=apply;document.getElementById("user-status-filter").onchange=apply;document.getElementById("user-reset").onclick=()=>{document.getElementById("user-search").value="";document.getElementById("user-role-filter").value="";document.getElementById("user-status-filter").value="";apply()}}
  SR.UsuariosModule={route:{path:"/usuarios",title:"Gestión de usuarios",render,bind}};
})();
